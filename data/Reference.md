Data format:

As JSON, an array of the following objects wherre one object corresponds to one actor.
```javascript
{
  name: String,
  movieName: String,
  followers: Number,
  following: Number,
  tweets: Number,
}
```

